name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
    paths:
      - "artifacts/dmtechy/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Build & Deploy DMTECHY

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build DMTECHY
        run: pnpm --filter @workspace/dmtechy run build:deploy

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy artifacts/dmtechy/dist --project-name=dmtechy --commit-dirty=true
